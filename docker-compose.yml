services:

  # PHP Service
  user_app:
    build:
      context: .
      dockerfile: Dockerfile
    image: digitalocean.com/php
    container_name: user_app
    restart: unless-stopped
    tty: true
    environment:
      SERVICE_NAME: user_app
      SERVICE_TAGS: dev
      UID: "${UID}"
      GID: "${GID}"
    working_dir: /var/www
    volumes:
      - ./:/var/www
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    user: "${UID}:${GID}"   # Run container as host user
    networks:
      - app-network

  # Nginx Service
  user_webserver:
    image: nginx:alpine
    container_name: user_webserver
    restart: unless-stopped
    tty: true
    ports:
      - "8000:80"
      - "443:443"
    volumes:
      - ./:/var/www
      - ./docker/nginx/conf.d/:/etc/nginx/conf.d/
    networks:
      - app-network

  # MySQL Service
  user_db:
    image: mysql
    container_name: user_db
    restart: unless-stopped
    tty: true
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: '${DB_DATABASE}'
      MYSQL_ROOT_PASSWORD: '${DB_PASSWORD}'
      SERVICE_TAGS: dev
      SERVICE_NAME: mysql
    networks:
      - app-network

# Docker Networks
networks:
  app-network:
    driver: bridge

# Volumes
volumes:
  data:
    driver: local